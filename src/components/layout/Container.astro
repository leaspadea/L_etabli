---

---

<div class="container">
  <slot />
</div>

<style lang="scss">
  @use "../../styles/abstracts/breakpoints" as bp;

  .container {
    max-width: 1280px;
    margin-inline: auto;
    padding-inline: 24px;

    @include bp.respond-to(desktop) {
      padding-inline: 32px;
    }
  }
</style>
